# process_nn 品質スコアカード

- 採点日: 2026-07-15（Asia/Tokyo）
- 対象ブランチ: `codex/technical-quality-pass`
- 比較基準: 2026-07-14 の数学教材追加後 → モデル評価教材・全ノート用語注釈追加後
- 採点の目的: 順位づけではなく、次の改善を同じ物差しで選ぶこと

## 結論

総合点は **8.7 → 9.1 / 10**。内部の非丸め加重平均は **8.74 → 9.06** である。

この点数はプロジェクトの絶対的価値ではない。以下の 5 軸、固定した重み、確認できる成果物と検証結果に基づく作業用 rubric である。未確認事項を満点扱いせず、改善で直接動いた軸だけを上げた。

| 軸 | 重み | 改善前 | 現在 | 根拠 |
|---|---:|---:|---:|---|
| 技術的正確性 | 30% | 8.8 | **8.9** | P0/P1修正と評価計算共通化に加え、注釈では歴史的由来と理解用の原語分解を区別し、断定範囲を限定 |
| 教材設計・導線 | 25% | 9.2 | **9.7** | 数学→測定の順路に加え、全77ノートから関係語3〜4件をその場で開き、中央Wikiへ往復できる注釈層を追加 |
| 検証・再現性 | 20% | 9.4 | **9.6** | 数値回帰に加え、112語の必須欄・ID一意性・全ノート割当・リンクを独立検証 |
| アクセシビリティ | 10% | 7.8 | **8.0** | 注釈をネイティブbutton/details、dialog名、Escape操作、モバイル1列で実装。全体WCAG監査は未実施なので加点を限定 |
| 根拠・限界の透明性 | 15% | 7.6 | **8.3** | 一次資料と評価境界に加え、語源欄の方針、原語分解との違い、代表的な原典をWiki内に明記 |

計算:

- 改善前: `8.8×0.30 + 9.2×0.25 + 9.4×0.20 + 7.8×0.10 + 7.6×0.15 = 8.74`
- 現在: `8.9×0.30 + 9.7×0.25 + 9.6×0.20 + 8.0×0.10 + 8.3×0.15 = 9.06`

## 今回「確実に上げられる」と判断した理由

### 0. 全ノートへ同じ注釈層を配れた

`viz/lib/term-notes.mjs` に112語を集約し、各語を次の独立した欄へ分けた。

- 原語・略称の部品
- 30秒の意味
- なぜ必要になったか
- Coffee Break（誤解・覚え方・小話）
- 歴史に触れる項目の原典

全76の `viz` ノートとGPT-2へ関係語を3〜4件ずつ割り当て、共有 `nav.js` と `term-notes.js` から同じドロワーを配る。中央の `terms.html` は全語検索、10カテゴリ絞り込み、固定リンク、関連ノートへの逆リンクを提供する。

内容回帰テストは、語数だけでなく、必須欄、ID一意性、参照ID、既存77ノートとの完全一致を検証する。個別ページの追加時に用語割当を忘れるとCIで検出できるため、単なる説明量の増加ではなく横断設計の改善として加点した。

### 1. 未実装の学習上の橋だった

既存の `metrics.html` は混同行列・適合率・再現率・F1、`crossval.html` は分割・リーク・交差検証を扱う。一方、次は未実装だった。

- 回帰指標の外れ値感度と単位
- ROCとPRが陽性率から受ける影響の違い
- 確率の順位と校正の違い
- 複数seedの対応のある差と区間
- ablation、validation、testの役割分担

新しい応用アルゴリズムではなく、既存の性能主張を読む力を補うため、76テーマ全体への波及が大きい。

### 2. 数値で合否を固定できる

`viz/lib/evaluation-metrics.mjs` に純粋関数を分離し、`tools/verify-content.mjs` で既知値と照合する。見た目だけの追加ではなく、説明と計算の再不一致をCIで検出できる。

### 3. 一次資料と解釈の境界を付けられる

- [scikit-learn: Metrics and scoring](https://scikit-learn.org/stable/modules/model_evaluation.html)
- [scikit-learn: Probability calibration](https://scikit-learn.org/stable/modules/calibration.html)
- [NIST: Confidence Limits for the Mean](https://www.itl.nist.gov/div898/handbook/eda/section3/eda352.htm)

教材では定義だけでなく、「Brierは校正だけを測るわけではない」「95%CIは今回の区間に95%確率で真値が入るという意味ではない」「testを選択へ使うとvalidationになる」という誤解しやすい境界も明示した。

## 点を上げなかった範囲

- GPT-2実重みの参照logits一致は未検証のため、正確性を大幅加点していない。
- 全80 HTMLの包括的WCAG監査は未実施のため、アクセシビリティは8.0に留めた。
- 全教材に一次資料・省略範囲・seedを統一表示していないため、透明性は満点から遠い。
- 全データ・全seedの収束保証はしていない。

## 次に点を上げやすい順

1. **全ページ共通の「このデモの前提」欄**: 一次資料、toy/実データ、省略、seed、一般化不可の値を構造化する。透明性と正確性へ直接効く。
2. **データ工程と学習診断**: 標準化、欠損、パイプライン内前処理、学習曲線、early stopping、OODを実計算でつなぐ。
3. **WCAGの機械監査＋代表ページの手動監査**: axe等の自動検査だけを「完全監査」とせず、キーボード、読み上げ、コントラスト、Canvas代替を分けて記録する。
4. **GPT-2参照検証**: 重みの取得方法とhashを固定し、層別テンソル・logits・生成を参照実装と比較する。

次の加点も、変更量ではなく「検査可能な証拠が増えたか」で判断する。
