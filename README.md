# process_nn — 機械学習の「学習過程」と「内部構造」を観る

機械学習が学ぶ様子を、フレームワークに頼らず**素の JavaScript で実装して可視化**する教育用プロジェクト。
**学習の過程＝構造の可視化**と**リアルタイムのビジュアライゼーション**を主眼に、いろいろな学習パラダイムをブラウザで 1 ステップずつ覗けます。

入口は **https://lutelute.github.io/process_nn/** （**学習ロードマップ**）。ビルド・インストール不要、ブラウザで開くだけ。トップは横並びの一覧ではなく、**初学者が「いま何を学び／前提は何で／次にどこへ」進めるか**を辿れる学習の流れ（推奨順路の背骨＋カテゴリ別の依存順）として再構成されています。

## 共通フォーマット：整理版リッチ・インフォグラフ

全ビューア（74 ページ＋GPT-2 ビューア）は、1 手法を**そのページだけで完結して理解できる「動くインフォグラフ」**に統一されています。基礎から、RAG・深層強化学習・GAN・拡散モデル・RLHF・MoE・PINN・説明可能AI（XAI）といった**応用トピック**までを同じ流儀で扱います。

- **単一ステージ・5 ステップ誘導** — 「次へ」で 問題 → 仕組み → 計算 → 学習/動作 → 結果 を 1 つずつ。一度に 1 焦点だけ提示し、認知負荷を抑える
- **操作バーは 1 本**（前へ／次へ／自動／最初から＋データ切替）。深掘り（数式の詳細・限界・関連手法）は折りたたみに収納
- **⚠ よくある誤解** — 各ページに「✗ 誤解 → ✓ 正しい理解」を 2 組。直感と逆の結果になるポイントに釘を刺す（全教材ページ＋GPT-2 に配備）
- **学習順路フッター** — ページ末尾に「← 前のテーマ／次のテーマ →」。読み終えたら地図に戻らず次へ進める
- **直感 → 数式（実数代入）→ フロー → 動作 → 結果 → 要点** を 1 画面で結び、人間がその手法を最後まで追えるよう設計

## 設計の 3 本柱（中身の質）

1. **実学習** — ランダムな重みではなく、その場で本当に学習する（中身が意味を持つ）
2. **実況解説** — 「今この瞬間に何が起きているか」を日本語で常時表示
3. **過程と結果の連動** — 学習で値・境界・構造が更新される様子、loss / 適応度などの推移を同時に見せる

**自動検証（GitHub Actions の CI で毎 push 実行）**: ① GPT-2 トークナイザ — `node tools/verify.mjs` で既知値（`encode("Hello world") = [15496, 995]`）と往復一致、② 全 76 ページの静的健全性 — `node tools/check-pages.mjs` でリンク切れ・インライン JS 構文をチェック。加えて実ブラウザでの実行時検証は `node tools/crawl-pages.mjs`（Playwright で全ページを開き console error / 未捕捉例外 / リクエスト失敗が 0 件であることを確認、ローカル実行用）。各ビューアの数値ロジックは開発時に Node で収束を確認しています。

---

## 🔗 ビューア一覧

### ① ニューラルネットの基礎
| ページ | 内容 |
|---|---|
| `index.html` | **学習ロードマップ**（推奨順路＋カテゴリ別の依存順）＋ **MLP 関数近似**デモ |
| `viz/map.html` | **手法マップ** — 全手法を性質（教師なし↔教師あり × 古典↔深層・言語）で 2 次元配置した俯瞰アトラス |
| `viz/index.html` | **信号の流れ** — 入力が層を流れ活性値で光る（合計の符号を実学習＋実況） |
| `viz/perceptron.html` | **単純パーセプトロン** — 誤分類点だけで境界を更新（誤り駆動）、XOR の限界→MLP へ |
| `viz/linreg.html` | **線形回帰（最小二乗）** — 残差の二乗和を最小化、勾配降下＝正規方程式の厳密解 |
| `viz/backprop.html` | **誤差逆伝播** — 順伝播→誤差→勾配の逆流→重み更新を 1 サイクル。結果の関数フィットも同時表示 |
| `viz/reg.html` | **正則化（L2）** — 過学習を λΣw² で抑制、誤差 vs λ の U 字で最適な複雑さ |
| `viz/dropout.html` | **ドロップアウト** — 訓練中に隠れ素子をランダムに落とす＝サブネットのアンサンブル（過学習対策） |
| `viz/vanishing.html` | **勾配消失問題** — 勾配は各層の微分の掛け算→sigmoid で指数的に消失、ReLU で安定（層ごと実測） |
| `viz/batchnorm.html` | **バッチ正規化** — 各層をバッチごとに平均0分散1へ正規化＋γβ。分布が安定し速く学習 |
| `viz/resnet.html` | **残差接続（ResNet）** — 差分だけ学び、skip を勾配が素通り。同条件32層の plain/skip 一騎打ちで層別勾配とlossを実測。勾配消失の解答編 |
| `viz/transfer.html` | **転移学習・ファインチューニング** — 土台を凍らせ頭だけ替える。少データ12点で「ゼロから 59% vs 凍結 99%」（微調整はデータ増で追い上げ）、データ量で優劣が逆転する曲線を実測 |
| `viz/distill.html` | **知識蒸留** — 先生の確率分布（ソフトラベル）は正解より情報が濃い。1/25 サイズの生徒が「正解のみ 64% vs 蒸留 78%」、暗黒知識と温度 T を実演。サロゲート②の親戚 |

### ② 教師あり学習 — 分類・認識
| ページ | 内容 |
|---|---|
| `viz/logreg.html` | **ロジスティック回帰** — シグモイドで確率化＋交差エントロピー。1 ニューロン＝NN の最小単位 |
| `viz/classify.html` | **決定境界** — 2 クラスを分ける境界が学習で形成（softmax+CE+Adam、データ 5 種） |
| `viz/digit.html` | **数字認識** — 描いた数字を認識し、判断の根拠（saliency）を実況 |
| `viz/cnn.html` | **CNN** — フィルタが画像を走査。🎓 で**フィルタを実学習**し特徴検出器を自力獲得 |
| `viz/rnn.html` | **RNN** — Elman+BPTT で時系列を 1 ステップ先予測、自由走行で波形再生 |
| `viz/hmm.html` | **隠れマルコフモデル（HMM）** — 見えない状態を見える出力列から推理。トレリスで forward / Viterbi を実計算、単時点推定 vs 筋読みの A/B |
| `viz/kalman.html` | **カルマンフィルタ** — 予測と観測を不確かさで重み付けして融合。観測/モデル/KF の三者対決 RMSE 実測、共分散楕円の膨張と収縮 |
| `viz/gnn.html` | **グラフNN（GNN）** — 「隣から集めて混ぜる」メッセージパッシングを可視化し、ラベル 2 個の半教師ありノード分類を GCN で実学習。グラフ5種（コミュニティ/送電網/鎖/格子=画像/分子）で「層数＝届く距離」「CNN=格子のGNN」も実証 |
| `viz/gnnflow.html` | **⚡特別講演: GNN × 潮流計算** — 全 15 枚の講演形式。6 バス系統の DC 潮流を本当に解き、P 再注入 GNN（5 層）がライブ学習で写し取る。未知ケースの潮流誤差 7%・過負荷判定一致 98%、外挿の危険まで実演。GNN×サロゲートの合流点 |
| `viz/pinn.html` | **物理情報 NN（PINN）** — 微分方程式を損失に足す。減衰振動の少数観測で、データの隙間・外挿の RMSE が 1〜2 桁改善（既定シーンで約 20 倍・約 50 倍）。λ の谷・コロケーション点の密度・逆問題（未知係数 a の同定）も実演 |
| `viz/dtree.html` | **決定木 (CART)** — 不純度が下がる軸で領域を再帰分割、木の成長＝特徴空間の分割（非NN） |
| `viz/forest.html` | **ランダムフォレスト** — ブートストラップ＋多数決で過学習を抑え滑らかに（アンサンブル） |
| `viz/boosting.html` | **勾配ブースティング** — 残差に弱学習器を逐次に当てて足す＝関数空間の勾配降下 |
| `viz/knn.html` | **k近傍法 (kNN)** — 学習しない事例ベース。★をドラッグし近傍の多数決を観察 |
| `viz/svm.html` | **SVM** — 最大マージンの境界を学習、サポートベクトルを可視化 |
| `viz/naivebayes.html` | **ナイーブベイズ** — 各クラスをガウスで表しベイズ則で分類（生成モデル）。事後確率を観察 |
| `viz/metrics.html` | **評価指標** — 混同行列→適合率/再現率/F1、しきい値のトレードオフ（分類器の測り方） |
| `viz/crossval.html` | **交差検証とデータの使い方** — holdout の運ゲー vs 5-fold の安定を選択次数の分布で実測。前処理リークの罠（見かけ 82% vs 実力 50%）も実演 |

### ③ 言語モデル — 埋め込みから Transformer・GPT へ
| ページ | 内容 |
|---|---|
| `viz/word2vec.html` | **単語埋め込み (word2vec)** — skip-gram で意味の近い語が空間上に集まる「意味の地図」 |
| `viz/positional.html` | **位置エンコーディング** — 波長違いの sin/cos で各位置を一意符号化、語順を自己注意に注入 |
| `viz/attention.html` | **自己注意（Self-Attention）** — Q·K·V の内積で各トークンが文中のどこを見るかを重み付け。Transformer・GPT の心臓部 |
| `viz/transformer.html` | **Transformer 学習** — 小型 GPT をブラウザで学習、loss 低下で attention・生成が変化 |
| `gpt2/` | **GPT-2 推論** — 本物の GPT-2 (124M) を forward 実行し、次トークン予測と内部を可視化 |
| `viz/lstm.html` | **LSTM（長短期記憶）** — セル状態と 3 ゲート（書く/保つ/読む）で長期依存を学習。最初の値が最後まで保たれる様子を可視化 |
| `viz/moe.html` | **Mixture of Experts（MoE）** — 専門家の集団＋受付（ルーター）。ゲート+4専門家の実学習で専門分化が創発、単一MLP比3.7倍改善、top-k の疎な計算と dead expert の A/B。Mixtral/GPT-4 の構造 |
| `viz/rag.html` | **RAG（検索拡張生成）** — 文書を意味ベクトル化→質問とコサイン類似で該当箇所を検索→出典つきで回答（昔話/論文/法的文書を選択） |

### ④ 教師なし学習 — 構造を見つける
| ページ | 内容 |
|---|---|
| `viz/kmeans.html` | **k-means** — 重心が収束しクラスタが現れる（Voronoi 背景・SSE 推移） |
| `viz/gmm.html` | **GMM（混合ガウス）** — EM 法でソフト割当、共分散楕円が収束、対数尤度が単調増加 |
| `viz/dbscan.html` | **DBSCAN** — 密度でクラスタ化。任意形状（三日月・同心円）OK、外れ値をノイズ除外 |
| `viz/hierarchical.html` | **階層的クラスタリング** — 近い塊を順に併合し家系図（デンドログラム）に。切断線ドラッグでクラスタ数は後決め、単連結 vs 完全連結の性格 A/B |
| `viz/autoencoder.html` | **オートエンコーダ** — 入力をボトルネックに圧縮して復元、圧縮の限界も見える |
| `viz/vae.html` | **VAE（変分AE）** — 潜在を N(0,1) に整え、z をサンプルして新データを**生成**（生成モデル） |
| `viz/gan.html` | **GAN（敵対的生成）** — 生成器 G と識別器 D を競わせ、G が本物分布に迫る（8 ガウスを実敵対学習・モード崩壊にも言及） |
| `viz/diffusion.html` | **拡散モデル（DDPM）** — データにノイズを足す逆を学び、ノイズから少しずつ除去して生成。Stable Diffusion の土台 |
| `viz/contrastive.html` | **対照学習（自己教師あり）** — ラベル無しで正例を近く・負例を遠くに（InfoNCE）。埋め込みがクラスタへ分離（SimCLR/CLIP 系） |
| `viz/pca.html` | **PCA** — 最大分散方向をべき乗反復で見つけ 1 次元へ圧縮 |
| `viz/tsne.html` | **t-SNE** — 高次元データを 2 次元へ、早期誇張でクラスタが浮かぶ |
| `viz/recommend.html` | **協調フィルタリング（推薦・行列分解）** — 穴だらけの評価行列を「ユーザー因子×アイテム因子」の積で埋める。SGD 実学習で欠損が埋まり、因子空間に映画が並ぶ。word2vec の親戚 |
| `viz/som.html` | **SOM（自己組織化マップ）** — 競合学習で格子がデータの形に広がり組織化 |
| `viz/hopfield.html` | **Hopfield ネットワーク** — パターンを記憶し、ノイズからエネルギー降下で想起（連想記憶） |
| `viz/anomaly.html` | **異常検知（Isolation Forest）** — 異常は孤立→ランダム分割で早く一人になる。中心からの距離では見逃す異常（順位139/144）を隔離が上位検出（7位）、AUC 0.83 vs 1.00 の直接対決。θ で見逃し⇔誤報の実測 |

### ⑤ 強化学習・最適化
| ページ | 内容 |
|---|---|
| `viz/bandit.html` | **多腕バンディット** — 探索と活用のジレンマの最小形。ε-greedy / UCB / Thompson を同一の運で実走し regret（機会損失）を比較 |
| `viz/qlearning.html` | **Q学習** — 報酬から最短経路を学習、価値が伝播し方策が定まる |
| `viz/dqn.html` | **DQN（深層強化学習）** — Q を表でなくニューラルネットで近似、経験再生＋ターゲット網で大きな状態でも学習 |
| `viz/policygrad.html` | **方策勾配（REINFORCE）** — 行動の確率を直接太らせる方策ベースの道。グリッドで実学習、ベースライン有無の安定性 A/B |
| `viz/actorcritic.html` | **Actor-Critic** — 動く人（方策）と批評家（価値）の分業、TD 誤差が共通の学習信号。REINFORCE との分散 9 倍差を実測 A/B |
| `viz/mcts.html` | **モンテカルロ木探索（MCTS）** — 有望な枝ほど深く読む（UCT）。三目並べで選択→展開→ロールアウト→逆伝播を実走、対戦成績を実測。AlphaGo の骨格 |
| `viz/rlhf.html` | **RLHF** — 好みの比較→Bradley-Terry 報酬モデル→方策改善+KL制約を全段ライブ実走。KL無しの報酬ハッキング（スパム化）を実演 |
| `viz/dronerace.html` | **ドローンレース × 深層強化学習** — ルール操縦（PD）の調整限界（16設定総当たりで実測）を、方策勾配＋批評家のライブ学習が追い抜く。最高速で負けたままラップで勝つ「線の発明」（内側掠め・ノーブレーキ通過を報酬だけから創発）。学習過程（墜落の残像・成長ギャラリー・迷いの減少）も可視化。Swift（Nature 2023）の構図の 2D 再現 |
| `viz/dronerace2.html` | **ドローンレース②：未知への強さ（分布シフト）** — ①の続編。菱形で学んだ方策は 8 の字コースで即墜落、無風学習は横風で 4 倍に激減速。**再学習**（複雑コースほど人間流との差が拡大）と**ドメインランダム化**（毎回ちがう風で頑健化・ただし訓練範囲まで）で乗り越える sim-to-real の入口 |
| `viz/gradient.html` | **勾配降下** — 損失地形を SGD/Momentum/Adam が降下し比較 |
| `viz/ga.html` | **遺伝的アルゴリズム** — 勾配なしで最適化、集団が選択・交叉・突然変異で大域最適へ進化 |
| `viz/surrogate.html` | **サロゲートモデル（代理モデル＋ベイズ最適化）** — 高価な関数を GP で「不確かさ付きの安い代理」に置き換え、獲得関数で次に測る 1 点を賢く選ぶ。グリッド探索との勝負つき（抽象3種＋NNの学習率・反応温度の実例）|
| `viz/surrogate2.html` | **サロゲートモデル②（ブラックボックスの複製）** — 開けられない箱を入出力だけから小型 NN で写し取り（実学習）、速くて微分できる替え玉に。内挿は信用・外挿は創作、を実証（共振・飽和・急変・バッテリー・半導体の5種）|
| `viz/gp.html` | **ガウス過程回帰** — 関数の上の確率分布。観測で帯が締まり、遠くは太る。厳密 GP（コレスキー）実計算、ℓ/σn の性格 A/B。ベイズ最適化の土台 |

### ⑥ 説明可能AI（XAI）— 根拠を測る
| ページ | 内容 |
|---|---|
| `viz/xai.html` | **XAI入門 — NN はどこを見たか** — saliency（勾配×入力）と occlusion（遮蔽）で判断根拠をマップ化。精度は同じでも「形を見るモデル」と「隅のショートカットを見るモデル」を証拠付きで見分ける |
| `viz/lime.html` | **LIME** — ブラックボックスを1点の近所だけ線形で写す局所説明。★をドラッグすると説明が変わる（局所性）、σ の罠とぶれも実測 |
| `viz/shap.html` | **SHAP** — 特徴の貢献を協力ゲームの公平な山分け（Shapley 値）で配る。おもちゃ審査モデルで厳密 Shapley を実計算、加法性誤差 1e-16、素朴帰属とのズレを実測 |

### ⑦ 立体で見る — 3D 構造
| ページ | 内容 |
|---|---|
| `viz/mlp3d.html` | MLP を層シートの積層として立体表示（実学習対応） |
| `viz/cnn3d.html` | CNN の特徴マップを H×W×チャンネルのボリュームで立体表示 |
| `viz/rnn3d.html` | RNN を時間方向に展開、BPTT で系列予測を実学習 |
| `viz/attention3d.html` | 本物の attention を 3D 弧で、ヘッドを奥行きに分離（minigpt 実学習） |
| `viz/gpt3d.html` | GPT-2 の 12 層 × 12 ヘッド構造を 3D で俯瞰、ブロック内部を展開 |

> 3D はライブラリ不使用。`viz/lib3d.js` が回転・弱透視投影・ドラッグ操作を担う自前 3D エンジン。

---

## `gpt2/` — GPT-2 の生成過程と内部構造ビューア

本物の **GPT-2 small（124M）をブラウザで forward 実行**し、生成を 1 トークンずつ進めながら内部を可視化する。`transformers.js` は logits しか返さないため、トークナイザと forward を自前実装している。

- `gpt2/index.html` — 次トークン確率（top-k）・**attention 行列**（層×ヘッド）・**logit lens**（層ごとの途中予測）・残差ストリームのノルム推移・各ブロックの寄与・MLP 発火、そして次トークン予測の実況
- `gpt2/tools/tokenizer.mjs` — GPT-2 byte-level BPE（Node/ブラウザ共通）
- `gpt2/tools/gpt2.mjs` — safetensors パーサ＋ forward エンジン（中間表現キャプチャ対応）
- `gpt2/tools/minigpt.mjs` — 学習可能な小型 Transformer（`viz/transformer.html`・`viz/attention3d.html` が使用）
- `gpt2/GPT_NOTES.md` — 実装リファレンス

> **重み本体 `model.safetensors`（約 523MB）はリポジトリに含めていません**。ビューアで「ローカルファイルを選択」か「HuggingFace から取得」で読み込みます。CLI で取得する場合:
> ```bash
> curl -L -o gpt2/assets/model.safetensors \
>   https://huggingface.co/gpt2/resolve/main/model.safetensors
> ```

#### 正しさの検証
- **トークナイザ**: `encode("Hello world") = [15496, 995]`（GPT-2 既知値）と往復一致を確認
- **forward**: 本物の重みで意味的に正しい予測を確認（例: "Water is made of hydrogen and" → " oxygen"、"The opposite of hot is" → " cold"）

---

## ローカルで開く

ES Modules と fetch を使うため、ファイル直開きではなくローカルサーバ経由で開いてください。

```bash
git clone https://github.com/lutelute/process_nn.git
cd process_nn
python3 -m http.server 8000
# → http://localhost:8000/        学習の地図ハブ + 関数近似デモ
# → http://localhost:8000/viz/    各種ビューア（信号フロー/分類/CNN/RNN/Transformer/教師なし/強化/3D ほか）
# → http://localhost:8000/gpt2/   GPT-2 推論ビューア
```

トップの `index.html` のみ外部依存ゼロの単体 HTML なので、直接ブラウザで開いても動きます。
